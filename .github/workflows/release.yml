# Thin stub — logic lives in rfizzle/concord (mod-release.yml).
name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    uses: rfizzle/concord/.github/workflows/mod-release.yml@master
    with:
      modrinth-id: "Bnp3Drhe"
      curseforge-id: "1591251"
    secrets: inherit
